listarefens = []
refem = 0

maiornumero = max(listarefens)
menornumero = min(listarefens)

    refem = int(input("Digite o número de reféns presos: "))
        listarefens.append(refem)
        while refem > 0:
            repeat()

    media:
    (maiornumero + menornumero / 2)
    print(media)


